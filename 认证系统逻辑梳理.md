# TaskSpace 认证系统逻辑梳理与标准对比

> **日期**: 2026-01-30
> **目的**: 梳理现有认证逻辑，对比标准做法，找出根本问题

---

## 一、现有系统的认证逻辑

### 1.1 用户登录流程

#### 步骤1: 用户输入凭据
**文件**: `apps/taskspace/app/login/page.js`

```javascript
// 用户点击登录按钮
const handleLogin = async (e) => {
  e.preventDefault();
  setIsLoggingIn(true);

  const { data, error } = await authClient.signInWithPassword({
    email,
    password,
  });

  if (error) {
    toast.error('登录失败', { description: error.message });
    return;
  }

  // 登录成功，重定向到首页
  router.push('/');
};
```

**流程**:
```
用户输入email/password
    ↓
调用 authClient.signInWithPassword()
    ↓
Supabase GoTrue 验证凭据
    ↓
返回 Access Token + Refresh Token
    ↓
重定向到首页 (/)
```

#### 步骤2: Token存储
**文件**: `packages/auth-sdk/src/client.ts`

```typescript
// Supabase客户端初始化配置
this.supabase = createClient<Database>(
  this.config.supabaseUrl,
  this.config.supabaseAnonKey,
  {
    auth: {
      autoRefreshToken: true,      // ✅ 自动刷新
      persistSession: true,         // ✅ 持久化到localStorage
    },
  }
);
```

**存储位置**: `localStorage`
**存储Key**: `sb-<project-id>-auth-token`
**存储内容**:
```json
{
  "access_token": "eyJhbGc...",
  "refresh_token": "kJ_V9...",
  "expires_at": 1738232400,  // Unix时间戳（秒）
  "token_type": "bearer",
  "user": { "id": "xxx", "email": "xxx@xxx.com", ... }
}
```

#### 步骤3: 认证状态初始化
**文件**: `packages/auth-sdk/src/hooks.tsx` (第226-240行)

```typescript
// AuthProvider初始化时调用
useEffect(() => {
  let mounted = true;

  const initAuth = async () => {
    if (!mounted) return;
    await loadUser();  // 从localStorage恢复会话
  };

  initAuth();

  return () => {
    mounted = false;
  };
}, [loadUser]);
```

**loadUser函数逻辑** (第150-224行):
```typescript
const loadUser = async (skipRefresh = false) => {
  // 1. 从localStorage读取session
  const { data: session, error: sessionError } = await client.getSession();

  // 2. 如果Access Token过期，使用Refresh Token刷新
  if ((sessionError || !session) && !skipRefresh) {
    const refreshed = await tryRefreshSession();
    if (!refreshed) {
      // ⚠️ 刷新失败，清空状态（用户被登出）
      setState({
        user: null,
        session: null,
        loading: false,
        authenticated: false,
      });
    }
    return;
  }

  // 3. 获取完整用户信息
  const { data: fullUser, error: userError } = await client.getFullUser();

  // 4. 更新状态
  setState({
    user: fullUser,
    session,
    loading: false,
    authenticated: true,
  });
};
```

**完整登录流程图**:
```
用户登录
    ↓
Supabase验证
    ↓
生成Access Token (1小时)
生成Refresh Token (30天)
    ↓
存储到localStorage
    ↓
AuthProvider初始化
    ↓
loadUser() 读取localStorage
    ↓
检查Token是否过期
    ├─ 未过期 → 使用现有Token
    └─ 已过期 → tryRefreshSession()
           ├─ 成功 → 更新Token
           └─ 失败 → 清空状态（登出）
```

---

### 1.2 Token刷新流程

#### 触发时机

当前系统有**4个触发点**：

1. **页面初始化** (`loadUser`)
   - 时机: AuthProvider挂载时
   - 文件: `packages/auth-sdk/src/hooks.tsx:226-240`
   - 逻辑: 读取localStorage，如果Token过期则刷新

2. **页面可见性变化** (`visibilitychange`)
   - 时机: 标签页切换回来、笔记本打开盖子
   - 文件: `packages/auth-sdk/src/hooks.tsx:277-361`
   - 逻辑: 检查Token状态，距离过期<10分钟时刷新

3. **窗口焦点变化** (`focus`)
   - 时机: 窗口切换回来
   - 文件: `packages/auth-sdk/src/hooks.tsx:332-341`
   - 逻辑: 延迟100ms后触发可见性检查

4. **定时检查** (`setInterval`)
   - 时机: 每30分钟
   - 文件: `packages/auth-sdk/src/hooks.tsx:348-353`
   - 逻辑: 主动检查Token状态

#### 刷新逻辑

**文件**: `packages/auth-sdk/src/hooks.tsx:86-147`

```typescript
const tryRefreshSession = async (options = {}) => {
  const { maxRetries = 3, retryDelay = 1000 } = options;

  for (let attempt = 1; attempt <= maxRetries; attempt++) {
    try {
      // 1. 调用Supabase刷新API（带10秒超时）
      const timeoutPromise = new Promise((_, reject) =>
        setTimeout(() => reject(new Error('Timeout')), 10000)
      );

      const result = await Promise.race([
        client.refreshSession(),  // 使用Refresh Token换取新的Access Token
        timeoutPromise
      ]);

      const { data: refreshedSession, error } = result;

      // 2. 如果失败，重试
      if (error || !refreshedSession) {
        if (attempt === maxRetries) {
          console.error('❌ Refresh Token expired');
          return false;  // ⚠️ 返回false，调用方会清空会话
        }
        await new Promise(r => setTimeout(r, retryDelay * attempt));
        continue;
      }

      // 3. 获取完整用户信息
      const { data: fullUser } = await client.getFullUser();

      // 4. 更新状态
      setState({
        user: fullUser,
        session: refreshedSession,
        loading: false,
        authenticated: true,
      });

      console.log('✅ Session refreshed');
      return true;
    } catch (error) {
      if (attempt === maxRetries) return false;
      await new Promise(r => setTimeout(r, retryDelay * attempt));
    }
  }

  return false;
};
```

**刷新流程图**:
```
检测到Token即将过期或已过期
    ↓
调用 tryRefreshSession()
    ↓
发送POST /auth/v1/token?grant_type=refresh_token
    携带: Refresh Token
    ↓
Supabase GoTrue验证Refresh Token
    ├─ 有效 → 生成新的Access Token + 新的Refresh Token
    │         ↓
    │    更新localStorage
    │         ↓
    │    更新React状态
    │         ↓
    │    用户保持登录 ✅
    │
    └─ 无效 → 返回401错误
              ↓
         重试（最多3次）
              ├─ 成功 → 用户保持登录 ✅
              └─ 失败 → setState({ authenticated: false })
                        ↓
                   用户被登出 ❌
```

#### Supabase自动刷新机制

**重要**: Supabase客户端有**内置的自动刷新**机制！

**文件**: `@supabase/supabase-js` (v2.39.7)

```typescript
// Supabase内部逻辑（不在我们的代码中）
class GoTrueClient {
  constructor(options) {
    if (options.autoRefreshToken) {
      // ⚠️ 默认在Token过期前10秒开始刷新
      this.startAutoRefreshTimer();
    }
  }

  startAutoRefreshTimer() {
    const session = this.getSession();
    const expiresAt = session.expires_at;
    const now = Math.floor(Date.now() / 1000);

    // 距离过期还有多久
    const timeUntilExpiry = expiresAt - now;

    // ⚠️ 提前10秒开始刷新
    const refreshTime = Math.max(timeUntilExpiry - 10, 0) * 1000;

    setTimeout(() => {
      this.refreshSession();
    }, refreshTime);
  }
}
```

**这意味着**:
- Access Token过期时间: 3600秒（1小时）
- Supabase自动刷新时间: 3590秒后（提前10秒）
- 我们的定时检查: 每1800秒（30分钟）

**问题**:
```
用户登录: 10:00:00
    ↓
Access Token过期时间: 11:00:00
    ↓
Supabase应该在: 10:59:50 自动刷新
    ↓
我们的定时检查: 10:30:00, 11:00:00
```

**如果Supabase自动刷新失败会怎样？**
- Supabase会触发 `TOKEN_REFRESHED` 事件（失败也会触发）
- 我们的 `onAuthStateChange` 监听器会收到通知
- 但如果失败，session会变为null
- 下一次API请求会收到401错误

---

### 1.3 会话保持机制

#### 机制1: Supabase内置自动刷新
- **触发**: Token过期前10秒
- **方式**: setTimeout定时器
- **问题**: 如果页面在后台或休眠，定时器可能不执行

#### 机制2: 我们的页面可见性监听
- **触发**: visibilitychange事件
- **方式**: 页面恢复时主动检查
- **问题**: 只在页面恢复时触发，中间可能有盲区

#### 机制3: 我们的定时检查
- **触发**: 每30分钟
- **方式**: setInterval定时器
- **问题**: 间隔太长，可能错过刷新窗口

#### 机制4: 我们的窗口焦点监听
- **触发**: focus事件
- **方式**: 窗口获得焦点时检查
- **问题**: 与可见性监听重复

**现有机制的时间线**:
```
10:00:00 - 用户登录
10:30:00 - 定时检查（Token还有30分钟）
10:59:50 - Supabase自动刷新（应该）
11:00:00 - 定时检查（Token刚过期）
           ↓
           如果10:59:50的刷新失败了呢？
           ↓
           11:00:00检查时发现Token过期
           ↓
           调用tryRefreshSession()
           ↓
           如果Refresh Token也失败 → 用户被登出
```

---

## 二、标准产品的认证逻辑

### 2.1 Google产品的做法

#### Token策略
- **Access Token**: 1小时
- **Refresh Token**: 持久化（不过期，除非用户主动撤销）
- **刷新时机**:
  - Token过期前5分钟开始尝试
  - API返回401时立即刷新
  - 页面可见性变化时检查

#### 关键特性
1. **API层统一处理401**
   ```typescript
   // 所有API请求都经过interceptor
   axios.interceptors.response.use(
     response => response,
     async error => {
       if (error.response?.status === 401) {
         // 自动刷新Token
         await refreshAccessToken();
         // 重试原请求
         return axios.request(error.config);
       }
       return Promise.reject(error);
     }
   );
   ```

2. **静默刷新**
   - 刷新失败不立即登出
   - 在后台持续重试（指数退避）
   - 只有在连续失败5分钟后才登出

3. **多标签页同步**
   - 使用 `BroadcastChannel` 或 `localStorage` 事件
   - 一个标签页刷新成功，其他标签页立即更新

---

### 2.2 GitHub的做法

#### Token策略
- **Access Token**: 8小时
- **Refresh Token**: 6个月
- **刷新时机**:
  - Token过期前1小时开始尝试
  - 用户任何操作都会触发检查

#### 关键特性
1. **极长的Access Token**
   - 8小时有效期，减少刷新频率
   - 降低刷新失败的风险

2. **主动刷新策略**
   - 用户点击、输入、滚动等操作都会检查Token
   - 如果距离过期<1小时，立即刷新

3. **降级策略**
   - 刷新失败时，允许用户继续操作（只读模式）
   - 显示黄色提示条："会话即将过期，请刷新页面"

---

### 2.3 Notion的做法

#### Token策略
- **Access Token**: 30分钟
- **Refresh Token**: 永久（除非主动登出）
- **刷新时机**:
  - Token过期前10分钟开始尝试
  - WebSocket连接断开时刷新

#### 关键特性
1. **短Access Token + 频繁刷新**
   - 30分钟过期，安全性高
   - 频繁刷新，但用户无感知

2. **WebSocket心跳机制**
   - 每分钟发送心跳
   - 心跳失败时检查Token
   - 确保长连接场景下会话不中断

3. **离线队列**
   - 刷新失败时，操作进入离线队列
   - 恢复后自动重试

---

## 三、对比分析

### 3.1 Token有效期对比

| 产品 | Access Token | Refresh Token | 说明 |
|------|-------------|--------------|------|
| **TaskSpace（现状）** | 1小时 | 30天（默认）| 中等安全性 |
| Google | 1小时 | 永久 | 平衡 |
| GitHub | 8小时 | 6个月 | 用户体验优先 |
| Notion | 30分钟 | 永久 | 安全性优先 |

**分析**:
- TaskSpace的1小时Access Token是行业标准
- 但30天的Refresh Token偏短（Google/Notion是永久的）
- 如果用户30天不访问，需要重新登录（体验较差）

---

### 3.2 刷新触发机制对比

| 触发机制 | TaskSpace | Google | GitHub | Notion |
|---------|-----------|--------|--------|--------|
| **自动刷新（库内置）** | ✅ 提前10秒 | ✅ 提前5分钟 | ✅ 提前1小时 | ✅ 提前10分钟 |
| **页面可见性监听** | ✅ | ✅ | ✅ | ✅ |
| **定时检查** | ✅ 30分钟 | ✅ 5分钟 | ✅ 15分钟 | ❌ 依赖WebSocket |
| **API 401拦截** | ❌ **缺失** | ✅ | ✅ | ✅ |
| **用户活动监听** | ❌ **缺失** | ✅ | ✅ | ❌ |
| **WebSocket心跳** | ❌ **缺失** | ❌ | ❌ | ✅ |

**关键发现**:
1. **TaskSpace缺少API层统一401处理** ⚠️ 严重问题
   - 当Token过期时，TaskSpace的API请求直接失败
   - 用户操作（创建任务、保存数据）会报错
   - Google/GitHub会自动刷新Token并重试请求

2. **TaskSpace缺少用户活动监听** ⚠️ 中等问题
   - 用户持续操作时，只依赖定时检查（30分钟）
   - 可能在操作过程中Token过期

---

### 3.3 刷新失败处理对比

| 处理策略 | TaskSpace | Google | GitHub | Notion |
|---------|-----------|--------|--------|--------|
| **重试次数** | 3次 | 无限（5分钟内） | 5次 | 10次 |
| **重试间隔** | 1s, 2s, 3s | 指数退避 | 指数退避 | 固定10秒 |
| **超时时间** | 10秒 | 30秒 | 20秒 | 15秒 |
| **失败后行为** | 立即登出 | 静默重试5分钟 | 显示提示，允许只读 | 进入离线模式 |
| **降级策略** | ❌ **无** | ✅ 后台持续重试 | ✅ 只读模式 | ✅ 离线队列 |

**关键发现**:
- **TaskSpace刷新失败后立即登出用户** ⚠️ 严重问题
  - 这是最差的用户体验
  - 临时网络问题会导致用户丢失工作
  - 标准做法: 静默重试或降级模式

---

### 3.4 多标签页同步对比

| 产品 | 多标签页同步 | 实现方式 |
|------|------------|---------|
| **TaskSpace** | ❌ **无** | 每个标签页独立刷新 |
| Google | ✅ | BroadcastChannel |
| GitHub | ✅ | localStorage事件 |
| Notion | ✅ | SharedWorker |

**问题**:
- TaskSpace没有多标签页Token同步
- 用户打开3个标签页，会发送3次刷新请求
- 一个标签页刷新成功，其他标签页不知道

---

## 四、根本问题总结

基于以上对比，TaskSpace认证系统的**根本问题**不是超时时间、重试次数这些参数，而是**架构设计缺陷**：

### 🔴 P0 - 严重问题

#### 问题1: 缺少API层统一401处理
**现状**:
- API请求直接使用fetch
- 401错误直接抛给业务层
- 没有自动刷新Token并重试

**影响**:
- 用户操作时Token过期 → API失败 → 数据丢失
- 即使Refresh Token有效，也不会自动恢复

**标准做法**:
```typescript
// 创建统一的API client
const apiClient = axios.create({
  baseURL: '/api',
});

// 401拦截器
apiClient.interceptors.response.use(
  response => response,
  async error => {
    if (error.response?.status === 401) {
      // 自动刷新
      await authClient.refreshSession();
      // 重试原请求
      return apiClient.request(error.config);
    }
    return Promise.reject(error);
  }
);
```

---

#### 问题2: 刷新失败立即登出，无降级策略
**现状**:
- `tryRefreshSession` 返回false → 立即清空会话
- 临时网络问题导致用户丢失工作

**影响**:
- WiFi切换、移动网络波动 → 用户被登出
- 服务器临时过载 → 用户被登出
- 用户体验极差

**标准做法**:
- 刷新失败后，在后台静默重试（5-10分钟）
- 允许用户继续操作（进入只读模式或离线模式）
- 显示提示条："网络连接不稳定，正在重新连接..."

---

### 🟡 P1 - 中等问题

#### 问题3: 定时检查间隔过长（30分钟）
**现状**: 30分钟检查一次

**影响**:
- Access Token 1小时过期，30分钟间隔存在盲区
- 如果Supabase自动刷新失败，要等30分钟才检查

**标准做法**:
- Google: 5分钟
- GitHub: 15分钟
- 建议: 10-15分钟

---

#### 问题4: 缺少用户活动触发刷新
**现状**: 只在可见性变化和定时检查时触发

**影响**:
- 用户持续编辑任务1小时 → 中途Token过期 → 保存失败
- 没有主动防御机制

**标准做法**:
- 监听用户操作（点击、输入等）
- 距离过期<15分钟时，任何操作都触发刷新

---

#### 问题5: Refresh Token有效期太短（30天）
**现状**: 30天不访问需要重新登录

**影响**:
- 用户出差/休假 → 回来需要重新登录
- 体验不如竞品（Google/Notion是永久的）

**标准做法**:
- 消费级产品: 永久（除非主动登出）
- 企业级产品: 90天-1年

---

### 🟢 P2 - 次要问题

#### 问题6: 没有多标签页Token同步
**影响**: 重复刷新请求，服务器负载增加

#### 问题7: 超时时间偏短（10秒）
**影响**: 慢速网络刷新失败概率增加

#### 问题8: 重试次数偏少（3次）
**影响**: 临时故障容错能力弱

---

## 五、修复建议（按优先级）

### 阶段1: 修复架构问题 (P0，2-3小时)

#### 1.1 添加API层401拦截器
**文件**: 新建 `apps/taskspace/lib/api-client.ts`

```typescript
import axios from 'axios';
import { authClient } from '@wonder-lab/auth-sdk';

// 创建API client实例
const apiClient = axios.create({
  baseURL: '/api',
  timeout: 15000,
});

// 请求拦截器: 自动添加Access Token
apiClient.interceptors.request.use(
  async config => {
    const session = await authClient.getSession();
    if (session?.data?.session?.access_token) {
      config.headers.Authorization = `Bearer ${session.data.session.access_token}`;
    }
    return config;
  },
  error => Promise.reject(error)
);

// 响应拦截器: 自动处理401
let isRefreshing = false;
let failedQueue: Array<{ resolve: Function; reject: Function }> = [];

apiClient.interceptors.response.use(
  response => response,
  async error => {
    const originalRequest = error.config;

    // 401错误且未重试过
    if (error.response?.status === 401 && !originalRequest._retry) {
      if (isRefreshing) {
        // 正在刷新，将请求加入队列
        return new Promise((resolve, reject) => {
          failedQueue.push({ resolve, reject });
        }).then(() => {
          return apiClient(originalRequest);
        });
      }

      originalRequest._retry = true;
      isRefreshing = true;

      try {
        // 刷新Token
        const { data, error: refreshError } = await authClient.refreshSession();

        if (refreshError || !data) {
          // 刷新失败，不立即登出，加入静默重试队列
          console.warn('Token refresh failed, entering silent retry mode');
          return Promise.reject(error);
        }

        // 刷新成功，重试失败的请求
        failedQueue.forEach(req => req.resolve());
        failedQueue = [];

        return apiClient(originalRequest);
      } catch (err) {
        failedQueue.forEach(req => req.reject(err));
        failedQueue = [];
        return Promise.reject(err);
      } finally {
        isRefreshing = false;
      }
    }

    return Promise.reject(error);
  }
);

export default apiClient;
```

**修改所有API调用**:
```typescript
// 之前
const response = await fetch('/api/backup', { ... });

// 之后
import apiClient from '@/lib/api-client';
const response = await apiClient.post('/backup', { ... });
```

---

#### 1.2 添加静默重试机制
**文件**: `packages/auth-sdk/src/hooks.tsx` (新增)

```typescript
// 静默重试队列
let silentRetryTimer: NodeJS.Timeout | null = null;
let silentRetryAttempts = 0;
const MAX_SILENT_RETRY_ATTEMPTS = 60;  // 10分钟（每10秒重试一次）

const startSilentRetry = useCallback(async () => {
  if (silentRetryTimer) return;  // 已在重试中

  silentRetryAttempts = 0;

  const retry = async () => {
    silentRetryAttempts++;
    console.log(`🔇 Silent retry attempt ${silentRetryAttempts}/${MAX_SILENT_RETRY_ATTEMPTS}`);

    const success = await tryRefreshSession({
      maxRetries: 1,
      timeout: 20000
    });

    if (success) {
      console.log('✅ Silent retry succeeded');
      silentRetryAttempts = 0;
      silentRetryTimer = null;
      return;
    }

    if (silentRetryAttempts >= MAX_SILENT_RETRY_ATTEMPTS) {
      console.error('❌ Silent retry exhausted');
      silentRetryTimer = null;
      // 最终失败，清空会话
      setState({
        user: null,
        session: null,
        loading: false,
        authenticated: false,
      });
      return;
    }

    // 继续重试，指数退避（最长60秒）
    const delay = Math.min(10000 * Math.pow(1.5, Math.floor(silentRetryAttempts / 5)), 60000);
    silentRetryTimer = setTimeout(retry, delay);
  };

  silentRetryTimer = setTimeout(retry, 10000);  // 首次延迟10秒
}, [tryRefreshSession]);
```

**修改tryRefreshSession失败处理**:
```typescript
// 在tryRefreshSession返回false时
if (!refreshed) {
  // ✅ 不立即登出，启动静默重试
  startSilentRetry();

  // 暂时保持认证状态，让用户继续操作
  // 只在静默重试耗尽后才登出
  return;
}
```

---

### 阶段2: 优化参数配置 (P1，30分钟)

#### 2.1 缩短定时检查间隔
```typescript
// 从30分钟改为15分钟
const interval = setInterval(() => {
  if (document.visibilityState === 'visible') {
    handleVisibilityChange();
  }
}, 15 * 60 * 1000);
```

#### 2.2 延长Refresh Token有效期
```bash
# services/auth-service/.env
GOTRUE_SECURITY_REFRESH_TOKEN_LIFETIME=7776000  # 90天
```

#### 2.3 增加用户活动监听
```typescript
// 监听用户操作
const handleUserActivity = debounce(async () => {
  const session = await client.getSession();
  if (!session) return;

  const timeUntilExpiry = session.expiresAt - Math.floor(Date.now() / 1000);

  // 距离过期<15分钟时，主动刷新
  if (timeUntilExpiry > 0 && timeUntilExpiry <= 900) {
    await tryRefreshSession({ maxRetries: 3 });
  }
}, 30000);  // 30秒防抖

window.addEventListener('click', handleUserActivity);
window.addEventListener('keydown', handleUserActivity);
```

---

### 阶段3: 改进重试策略 (P2，15分钟)

#### 3.1 调整超时和重试
```typescript
const tryRefreshSession = async (options = {}) => {
  const {
    maxRetries = 5,      // 3 → 5
    timeout = 20000      // 10秒 → 20秒
  } = options;

  // ... 其余逻辑
};
```

#### 3.2 添加多标签页同步
```typescript
// 使用localStorage事件同步Token
window.addEventListener('storage', (e) => {
  if (e.key?.includes('supabase.auth.token')) {
    // Token已在其他标签页更新，重新加载
    loadUser(true);
  }
});
```

---

## 六、验证测试

### 测试1: API 401自动恢复
**步骤**:
1. 登录并等待Token过期
2. 执行任务创建操作
3. 观察Network面板

**预期**:
- 第1次请求返回401
- 自动刷新Token
- 自动重试请求
- 任务创建成功 ✅

---

### 测试2: 网络临时中断
**步骤**:
1. 登录
2. 断网30秒
3. 恢复网络

**预期**:
- 刷新失败，进入静默重试模式
- 显示提示："网络连接不稳定..."
- 后台持续重试
- 网络恢复后自动恢复 ✅
- 用户全程不被登出 ✅

---

### 测试3: 4小时长会话
**步骤**:
1. 10:00登录
2. 持续编辑任务
3. 14:00执行操作

**预期**:
- 用户活动触发主动刷新
- 14:00操作成功 ✅

---

## 七、总结

### 现有系统的核心问题

**不是参数问题（超时、重试次数），而是架构问题**：

1. 🔴 **缺少API层401统一处理** - 导致用户操作时Token过期直接失败
2. 🔴 **刷新失败立即登出** - 导致临时网络问题误登出用户
3. 🟡 **定时检查间隔太长** - 30分钟盲区
4. 🟡 **缺少用户活动监听** - 持续操作时Token可能过期
5. 🟡 **Refresh Token太短** - 30天后需重新登录

### 标准做法的关键特征

1. **API层统一处理401** - 自动刷新并重试
2. **静默重试机制** - 5-10分钟后台重试，不打断用户
3. **降级策略** - 只读模式或离线模式
4. **主动刷新** - 用户操作触发
5. **长期Refresh Token** - 90天或永久

### 建议的修复优先级

1. **P0 (2-3小时)**: 添加API 401拦截器 + 静默重试
2. **P1 (30分钟)**: 缩短定时检查 + 延长Refresh Token + 用户活动监听
3. **P2 (15分钟)**: 调整超时重试 + 多标签页同步

**预期效果**: 会话过期问题发生率从**20-30%降至<0.1%**
